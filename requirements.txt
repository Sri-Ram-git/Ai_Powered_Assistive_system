# Core dependencies
opencv-python>=4.9.0
numpy>=1.24.0

# Professional HUD text rendering (TTF fonts, antialiasing)
Pillow>=10.0.0

# Plotting / visualisation (used in later modules)
matplotlib>=3.7.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Utilities
PyYAML>=6.0
psutil>=5.9.0

# Week 2-3 AI modules
# ONNX runtime (CPU) for YOLO + RapidOCR inference
onnxruntime>=1.18.0

# Text recognition (CPU-only, no Paddle/PyTorch)
rapidocr-onnxruntime>=1.2.0

# Text-to-speech (uses the OS speech engine: SAPI5 on Windows)
pyttsx3>=2.90

# Web dashboard (live camera + state via browser)
Flask>=3.0.0
flask-cors>=4.0.0
